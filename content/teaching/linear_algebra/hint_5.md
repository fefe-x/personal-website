# Hint 1.5

A basis for C(A) is given by the columns of A that have a pivot in row echelon form.

A basis for N(A) can be attained through solving Ax=0 and for each free variable taking one vector consisting of the coefficients of that free variable in the general solution vector.
